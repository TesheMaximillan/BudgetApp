<%= form_for @category, method: :post, data: {turbo: false} do |f| %>
  <div class="form-group">
    <div class="form-group__field">
      <%= f.label :name, class: "hidden" %>
      <%= f.text_field :name, autofocus: true, required: true, autocomplete: "name", placeholder: "Name", class: 'form-group__field--input' %>
    </div>

    <div class="form-group__field">
      <% if controller_name == 'categories' %>
        <%= f.label :name, class: "hidden" %>
        <%= f.file_field :icon, class: "form-group__field--file" %>
      <% else %>
        <%= f.label :title, class: "hidden" %>
        <%= f.number_field :amount, class: 'form-group__field--number' %>
      <% end %>
    </div>

    <div class="form-group__actions">
      <% if controller_name == 'categories' %>
        <%= f.submit "Create", class: "submit-btn category-btn" %>
      <% else %>
        <%= f.submit "Update", class: "submit-btn" %>
      <% end %>
    </div>
  </div>
<% end %>