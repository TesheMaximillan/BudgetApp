<%= form_for @category, method: :post, data: {turbo: false} do |f| %>
  <div class="form-group">
    <div class="form-group__field">
      <%= f.label :name, class: "hidden" %>
      <%= f.text_field :name, autofocus: true, required: true, autocomplete: "name", class: 'form-group__field--input' %>
    </div>

    <div class="form-group__field">
      <% if controller_name == 'categories' %>
        <%= f.label :name, class: "hidden" %>
        <%= f.file_field :icon %>
      <% else %>
        <%= f.label :title, class: "hidden" %>
        <%= f.number_field :amount, class: 'form-group__field--file' %>
      <% end %>
    </div>

    <div class="form-group__actions">
      <%= f.submit "Create", class: "submit-btn" %>
    </div>
  </div>
<% end %>